name: API Integration Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      NODE_ENV: development
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Clear npm cache
        run: npm cache clean --force

      - name: Install dependencies
        run: |
          # Install with verbose logging; include optional deps so Next SWC binaries are available
          npm ci --include=dev --verbose > npm-install.log 2>&1 || (
            echo "❌ npm install failed. Showing install log:"
            cat npm-install.log
            exit 1
          )

      - name: Install wait-on globally
        run: npm install -g wait-on

      - name: Build
        env:
          NODE_ENV: production
        run: |
          npm run build --verbose || (
            echo "❌ Build failed. Showing npm install log for debugging:"
            cat npm-install.log
            exit 1
          )

      - name: Start Next.js production server in background
        env:
          NODE_ENV: production
        run: |
          # Build already run above; start the production server for stability in CI
          # Bind to localhost for more reliable connections
          NODE_ENV=production NEXT_TELEMETRY_DISABLED=1 DEBUG=* npm run start -- -p 3000 --hostname localhost > .next.log 2>&1 &
          echo "Started production server (background)"
          # Give process a moment to crash if it's going to
          sleep 5
          if ! ps aux | grep -v grep | grep "next start"; then
            echo "❌ Server crashed immediately:"
            cat .next.log
            exit 1
          fi

      - name: Wait for Next.js to be ready
        run: |
          echo "Waiting up to 300s for tcp:3000 (accept any HTTP status) ..."
          # Use TCP readiness instead of HTTP 2xx to avoid false negatives when root returns 404
          wait-on tcp:3000 --interval 2000 --timeout 300000 || (
            echo "❌ Server did not respond within 300s. Printing logs for debugging:"
            echo "---- .next.log ----"
            tail -n 200 .next.log || echo "No .next.log found"
            echo "---- .dev.log ----"
            tail -n 200 .dev.log || echo "No .dev.log found"
            exit 1
          )

      - name: Confirm Supabase envs loaded
        run: |
          echo "NEXT_PUBLIC_SUPABASE_URL: ${NEXT_PUBLIC_SUPABASE_URL:-not set}"
          echo "NEXT_PUBLIC_SUPABASE_ANON_KEY: ${NEXT_PUBLIC_SUPABASE_ANON_KEY:+[present]}"
          echo "SUPABASE_SERVICE_ROLE_KEY: ${SUPABASE_SERVICE_ROLE_KEY:+[present]}"

      - name: Verify agent_availability table exists
        run: |
          echo "🔍 Checking if agent_availability table exists in Supabase..."
          curl -s -X GET "${NEXT_PUBLIC_SUPABASE_URL}/rest/v1/agent_availability?limit=1" \
            -H "apikey: ${NEXT_PUBLIC_SUPABASE_ANON_KEY}" \
            -H "Authorization: Bearer ${SUPABASE_SERVICE_ROLE_KEY}" \
            -H "Accept: application/json" || echo "⚠️  Table check failed - table may not exist"

      - name: Run API integration tests
        id: run_tests
        continue-on-error: true
        run: |
          echo "Running npm run test:api"
          npm run test:api 2>&1 | tee test-output.log
          exit ${PIPESTATUS[0]}

      - name: Show test output on failure
        if: steps.run_tests.outcome == 'failure'
        run: |
          echo "=========================================="
          echo "TEST FAILED - Full Output:"
          echo "=========================================="
          cat test-output.log || echo "No test output captured"
          echo "=========================================="
      
      - name: Fail workflow if tests failed
        if: steps.run_tests.outcome == 'failure'
        run: exit 1

      - name: Show Next.js logs (if any)
        if: failure()
        run: |
          echo "---- .dev.log (tail) ----"
          [ -f .dev.log ] && tail -n 200 .dev.log || true
